import { TdFixedDirective } from './td-fixed.directive';

describe('TdFixedDirective', () => {
  it('should create an instance', () => {
    const directive = new TdFixedDirective();
    expect(directive).toBeTruthy();
  });
});
