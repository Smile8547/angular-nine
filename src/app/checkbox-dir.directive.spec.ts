import { CheckboxDirDirective } from './checkbox-dir.directive';

describe('CheckboxDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxDirDirective();
    expect(directive).toBeTruthy();
  });
});
